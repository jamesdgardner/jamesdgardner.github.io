---
title: "Lecture 13 Class Notes"
format: html
editor: visual
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
1 + 1
```

You can add options to executable code like this

```{r}
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).

the tendency for the values of two or more variables to vary together in a **related** way

# This is James Gardner's Lecture Notes for Lecture 13 on 3/19/2024 (tuesday class)
```{r}
library(tidyverse)
library(skimr)
library(rmarkdown)

ggplot(data = mpg) +
  geom_point(mapping = 
               aes(x = displ,
                   y = hwy),
             alpha = .3) +
  geom_smooth(mapping =
                aes(x = displ,
                    y = hwy))

```
# This is next part uses a histogram
```{r}
ggplot(data = mpg) +
  geom_histogram(mapping =
                   aes(x = displ))
```
```{r}
ggplot(data = mpg) +
  geom_freqpoly(mapping = 
                  aes(x = displ))
```
```{r}
ggplot(data = mpg) +
  geom_histogram(mapping =
                    aes(x = displ, bins = 5))
```
```{r}
ggplot(data = mpg) +
  geom_histogram(mapping =
                   aes(x = displ, binwidth = 1))
```
# describing a distribution with boxplots
```{r}
ggplot(data = mpg) +
  geom_boxplot(mapping =
                 aes( x = class,
               y = hwy))
```
# Describing a Distribution with Bar Charts
```{r}
ggplot(data = diamonds) +
  geom_bar(mapping =
             aes(x = cut))
```
# Describing Time Trend with Line Charts
```{r}
path <- 
  "https://bcdanl.github.io/data/NVDA.csv"
nvda <- read_csv(path)

ggplot(data = nvda) + 
  geom_line(mapping = 
                aes(x = Date, 
                    y = Close))
```











